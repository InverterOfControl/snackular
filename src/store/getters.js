export const snackTypes = state => state.snackTypes

export const allSnacks = state => state.takenSnacks

export const allCompensations = state => state.compensations

export const totalBalance = state => {
  // calculate snacksum
  // add all compensations

  // return difference
}
